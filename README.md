# Recipe Search App (React + API)

A modern Recipe Search Application built with React, allowing users to search for recipes, view details, and save favorites.

This project demonstrates API integration, global state management using React Context, and responsive UI built with TailwindCSS.

